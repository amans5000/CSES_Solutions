#include <bits/stdc++.h>
using namespace std;

int main()
{
  int n, x;
  cin >> x >> n;

  vector<int> signal(n);
  for (int i = 0; i < n; i++)
  {
    cin >> signal[i];
  }

  vector<int> arr(x + 1, 0);
  int maxCount = 0;

  int i = 0;
  while (n--)
  {
    int mid = i++;
    
  }

  return 0;
}
